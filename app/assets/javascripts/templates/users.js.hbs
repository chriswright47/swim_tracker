<div class="row">
  <div class="users col-xs-6">
    <h1>Users</h1>
      <ul class="list-unstyled">
        {{#each user in controller}}
          {{#link-to 'user' user tagName="li" class="btn"}}
            {{user.fullName}}
          {{/link-to}}
          <br/>
        {{/each}}
      </ul>
  </div>
  <div class="cols-xs-6">
    {{outlet}}
  </div>
</div>