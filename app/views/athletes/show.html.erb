<h1><%= @athlete.full_name %></h1>
<%= link_to 'Edit athlete', edit_athlete_path(@athlete), :class => 'btn btn-info' %>
<%= link_to 'Delete athlete', @athlete, :method => :delete, :class => 'btn btn-danger', :"data-confirm" => "Are you sure you want to delete #{@athlete.full_name}" %>
