<div class="row">
  <%= link_to 'Create a new meet', new_meet_path, {:class => 'btn btn-info'} %>
</div>
<br/>
<div class="row">
  <div class="col-xs-12">
    <ul class="list-unstyled">
      <% @meets.each do |meet| %>
        <li class="btn">
          <%= link_to meet.title, meet %>
        </li>
        <br/>
      <% end %>
    </ul>
</div>